<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.simplejavamail</groupId>
		<artifactId>smtp-connection-pool-parent</artifactId>
		<version>4.0.1</version>
	</parent>

	<artifactId>smtp-connection-pool-demo</artifactId>
	<packaging>jar</packaging>
	<name>SMTP Connection Pool :: Executable Demos</name>
	<description>Non-published, executable demonstrations of the supported SMTP connection-pool usage paths.</description>

	<properties>
		<automaticModuleName>org.simplejavamail.smtpconnectionpool.demo</automaticModuleName>
		<java.version>17</java.version>
		<maven.compiler.release>17</maven.compiler.release>
		<maven.deploy.skip>true</maven.deploy.skip>
		<maven.javadoc.skip>true</maven.javadoc.skip>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.simplejavamail</groupId>
			<artifactId>smtp-connection-pool</artifactId>
		</dependency>
		<dependency>
			<groupId>org.simplejavamail</groupId>
			<artifactId>smtp-connection-pool-jakarta-provider</artifactId>
		</dependency>
		<dependency>
			<groupId>org.simplejavamail</groupId>
			<artifactId>smtp-connection-pool-camel</artifactId>
		</dependency>

		<!-- Simple Java Mail demonstrates a higher-level library built directly on the pool. -->
		<dependency>
			<groupId>org.simplejavamail</groupId>
			<artifactId>simple-java-mail</artifactId>
		</dependency>
		<!-- Also used directly by BatchModuleDemo through the public API introduced in Simple Java Mail 9.3.0. -->
		<dependency>
			<groupId>org.simplejavamail</groupId>
			<artifactId>batch-module</artifactId>
		</dependency>

		<dependency>
			<groupId>org.eclipse.angus</groupId>
			<artifactId>angus-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context-support</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.camel</groupId>
			<artifactId>camel-core</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.davidmoten</groupId>
			<artifactId>subethasmtp</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>3.8.1</version>
				<executions>
					<execution>
						<id>build-jpms-module-path</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>build-classpath</goal>
						</goals>
						<configuration>
							<includeArtifactIds>jakarta.mail-api,jakarta.activation-api,generic-object-pool,clustered-object-pool,smtp-connection-pool,smtp-connection-pool-jakarta-provider,smtp-connection-pool-camel,batch-module</includeArtifactIds>
							<includeScope>compile</includeScope>
							<outputFile>${project.build.directory}/jpms-module-path.txt</outputFile>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>verify-jpms-module-path</id>
						<phase>verify</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<loadfile property="jpmsModulePath"
										  srcFile="${project.build.directory}/jpms-module-path.txt" />
								<mkdir dir="${project.build.directory}/jpms-consumer-classes" />
								<javac srcdir="${project.basedir}/src/test/jpms"
									   destdir="${project.build.directory}/jpms-consumer-classes"
									   modulepath="${jpmsModulePath}"
									   release="17"
									   includeantruntime="false" />
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
